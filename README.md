# Love PWA - Приложение для знакомств

PWA приложение для знакомств на Laravel + JavaScript.

## Возможности

- 🔐 Аутентификация через Laravel Sanctum и Telegram
- 👤 Профиль пользователя с фото, интересами, городом, возрастом, полом
- 🔍 **Поиск собеседников по интересам, возрасту, полу, городу**
- ❤️ **Система оценки пользователей (лайки/дизлайки)** с анимациями
- 💬 **Обмен сообщениями между пользователями** с историей чата
- 📱 Progressive Web App с оффлайн поддержкой

## Установка

1. Установите зависимости:

```bash
composer install
npm install
```

2. Создайте файл `.env` из `.env.example`:

```bash
cp .env.example .env
php artisan key:generate
```

3. Создайте базу данных SQLite:

```bash
touch database/database.sqlite
```

4. Запустите миграции:

```bash
php artisan migrate
```

5. Создайте символическую ссылку для хранилища:

```bash
php artisan storage:link
```

6. Запустите сервер разработки:

```bash
php artisan serve
npm run dev
```

## Настройка Telegram

1. Создайте бота через [@BotFather](https://t.me/BotFather)
2. Получите токен бота
3. Добавьте токен в `.env`:

```
TELEGRAM_BOT_TOKEN=your_bot_token
TELEGRAM_BOT_USERNAME=your_bot_username
```

## Структура проекта

- `app/Http/Controllers` - API контроллеры
- `app/Models` - Модели Eloquent
- `resources/js` - JavaScript код фронтенда
- `resources/css` - Стили
- `public/manifest.json` - PWA манифест
- `public/sw.js` - Service Worker

## API Endpoints

### Аутентификация
- `POST /api/register` - Регистрация
- `POST /api/login` - Вход
- `POST /api/telegram-auth` - Вход через Telegram
- `POST /api/logout` - Выход
- `GET /api/me` - Текущий пользователь

### Профиль
- `GET /api/profile` - Получить профиль
- `PUT /api/profile` - Обновить профиль
- `GET /api/discover` - Поиск пользователей

### Лайки
- `POST /api/like` - Лайкнуть пользователя
- `POST /api/dislike` - Дизлайкнуть пользователя
- `GET /api/matches` - Получить совпадения

### Сообщения
- `GET /api/conversations` - Список чатов
- `GET /api/messages/{matchId}` - Сообщения в чате
- `POST /api/messages` - Отправить сообщение

## Обновление проекта

Для обновления проекта без простоя используйте:

```bash
# Автоматическое обновление
chmod +x deploy.sh
./deploy.sh

# Или вручную
git pull origin main
composer install --no-dev --optimize-autoloader
npm install && npm run build
php artisan migrate --force
php artisan optimize
```

Подробные инструкции по обновлению см. в [UPDATE.md](UPDATE.md)

## Отслеживание изменений

Все изменения проекта документируются в папке `docs/`:
- **docs/CHANGELOG.md** - История изменений в формате Keep a Changelog
- **docs/UPDATE_LOG.md** - Детальный журнал всех обновлений

Для добавления записи об изменении:

```bash
# Интерактивный режим
npm run log "Описание изменения"

# Или с типом изменения
npm run log:add "Добавлена новая функция"
npm run log:fix "Исправлен баг в поиске"
npm run log:change "Улучшен интерфейс чата"
```

## Установка в Open Server Panel

Подробная инструкция по установке в Open Server Panel 6.4.6: [docs/OPEN_SERVER_SETUP.md](docs/OPEN_SERVER_SETUP.md)

## Лицензия

MIT

